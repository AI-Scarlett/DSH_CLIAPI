# Optional manual entry. The one-command installer registers plugin/cordis.patch.yml
# as a standard Harness bundle, so normal installations do not merge this file.
- insert:
    - id: dsh-cliapi
      name: '@local/dsh-cliapi'
      config: {}
